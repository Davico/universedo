---
const testimonials = [
    {
        name: "Ivan M. Dunavski",
        role: "Strukovni fizioterpeut Msc. Medicinski radiolog",
        image: "dunavski_testimonial.jpeg",
        rating: 5,
        text: "Sarađujemo 7 godina . Radio mi je nekoliko sajtova. Trenutno mi odrzava sve sajtove. Visoko profesionalan, odgovoran, svaki problem na sajtu rešava u kratkom vremenu.",
        date: "6 months ago"
    },
    {
        name: "Saša Vukmirović",
        role: "Prof.dr sc. med. spec. farmakologije",
        image: "sasa_testimonial.jpg",
        rating: 5,
        text: "Izuzetno posvećen svom radu i klijentima. Uvek je dostupan za konsultacije i pruža stručne savete koji su od velike pomoći. Sposoban je da razume potrebe klijenata i prilagodi rešenja tim potrebama.",
        date: "4 months ago"
    },
    {
        name: "Marc Petersen",
        role: "CEO/CTO, Raisenext",
        image: "marc_testimonial.jpeg",
        rating: 5,
        text: "One of the most trustworthy developers we have collaborated with. Our team is excited to have had the pleasure to work with David on our project. His knowledge surpasses expectations, communication is excellent. He can quickly analyze the problem, and find proper solution. Please do not hesitate to hire David - he will execute and deliver.",
        date: "1 month ago"
    }
];
---

<section class="testimonials" id="testimonials">
    <div class="testimonials-container">
        <div class="section-header">
            <h2>What Other Professionals Say About Working With Us</h2>
            <p>Don't just take our word for it. Here's what real clients have achieved:</p>
        </div>

        <div class="testimonials-grid">
            {testimonials.map(testimonial => (
                <div class="testimonial-card">
                    <div class="testimonial-header">
                        <div class="client-info">
                            <div class="avatar">
                                <img class="avatar-img" src={`/${testimonial.image}`} alt={testimonial.name} />
                            </div>
                            <div class="client-details">
                                <h4>{testimonial.name}</h4>
                                <p class="role">{testimonial.role}</p>
                            </div>
                        </div>
                        <div class="rating">
                            {Array.from({ length: testimonial.rating }).map(() => (
                                <span class="star">★</span>
                            ))}
                        </div>
                    </div>
                    <p class="testimonial-text">"{testimonial.text}"</p>
                    <p class="testimonial-date">{testimonial.date}</p>
                </div>
            ))}
        </div>

        <div class="cta-container">
            <h3>Ready to Transform Your Online Presence?</h3>
            <a href="mailto:office.universedo@gmail.com" target="_blank" class="primary-cta">
                Book Your Strategy Call →
            </a>
        </div>
    </div>
</section>

<style>
    .testimonials {
        background: linear-gradient(135deg, var(--bg-primary) 0%, var(--color-bg-accent) 100%);
        padding: 4rem 2rem;
    }

    .testimonials-container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .section-header {
        text-align: center;
        margin-bottom: 3rem;
    }

    .section-header h2 {
        font-size: 2.5rem;
        color: var(--text-primary);
        margin-bottom: 1rem;
        font-weight: 700;
    }

    .section-header p {
        font-size: 1.1rem;
        color: var(--text-secondary);
    }

    .testimonials-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 2rem;
        margin-bottom: 3rem;
    }

    .testimonial-card {
        background: var(--bg-primary);
        padding: 2rem;
        border-radius: 12px;
        box-shadow: var(--shadow-md);
        transition: all 0.3s;
        border: 1px solid var(--color-border);
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .testimonial-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 30px rgba(155,55,242, 0.15);
        border-color: var(--color-primary);
    }

    .testimonial-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 0.5rem;
    }

    .client-info {
        display: flex;
        gap: 1rem;
        align-items: center;
    }

    .avatar {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 1rem;
        flex-shrink: 0;
    }

    .avatar-img {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        object-fit: cover;
    }

    .client-details h4 {
        font-size: 1.1rem;
        color: var(--text-primary);
        margin-bottom: 0.2rem;
        font-weight: 600;
    }

    .role {
        font-size: 0.9rem;
        color: var(--text-secondary);
        margin: 0;
    }

    .rating {
        display: flex;
        gap: 0.2rem;
    }

    .star {
        color: #FFB800;
        font-size: 1.2rem;
    }

    .testimonial-text {
        color: var(--text-primary);
        line-height: 1.7;
        font-size: 1rem;
        flex-grow: 1;
        font-style: italic;
    }

    .testimonial-date {
        font-size: 0.85rem;
        color: var(--text-secondary);
        margin: 0;
    }

    .testimonials-stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 2rem;
        margin: 3rem 0;
        padding: 2rem;
        background: var(--bg-primary);
        border-radius: 12px;
        box-shadow: var(--shadow-md);
        border: 1px solid var(--color-border);
    }

    .stat {
        text-align: center;
    }

    .stat-number {
        font-size: 3rem;
        color: var(--color-primary);
        font-weight: 700;
        line-height: 1;
        margin-bottom: 0.5rem;
    }

    .stat-stars {
        color: #FFB800;
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
    }

    .stat-label {
        color: var(--text-secondary);
        font-size: 0.95rem;
    }

    .cta-container {
        text-align: center;
        margin-top: 3rem;
        padding: 2rem;
        background: var(--bg-primary);
        border-radius: 12px;
        box-shadow: var(--shadow-sm);
        border: 2px dashed var(--color-primary);
    }

    .cta-container h3 {
        font-size: 1.8rem;
        color: var(--text-primary);
        margin-bottom: 1.5rem;
    }

    .primary-cta {
        display: inline-block;
        background: var(--color-primary);
        color: white;
        padding: 1rem 2.5rem;
        border-radius: 8px;
        font-weight: 600;
        font-size: 1.1rem;
        transition: all 0.3s;
        text-decoration: none;
    }

    .primary-cta:hover {
        background: var(--color-primary-dark);
        transform: translateY(-3px);
        box-shadow: 0 6px 20px rgba(155,55,242, 0.2);
    }

    @media (max-width: 768px) {
        .testimonials {
            padding: 2rem 1rem;
        }

        .section-header h2 {
            font-size: 1.8rem;
        }

        .testimonials-grid {
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        .testimonial-card {
            padding: 1.5rem;
        }

        .stat-number {
            font-size: 2.5rem;
        }

        .cta-container h3 {
            font-size: 1.4rem;
        }

        .primary-cta {
            width: 100%;
            padding: 1rem 2rem;
        }
    }
</style>